# This file is for reference only
# The actual API key should be set as a secret in Cloudflare Pages dashboard
# See DEPLOYMENT.md for instructions

# REBRICKABLE_API_KEY will be set in Cloudflare Pages as an environment variable
# NOT VITE_REBRICKABLE_API_KEY (we're using server-side proxy now)
