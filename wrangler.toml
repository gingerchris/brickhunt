name = "brickhunt"
compatibility_date = "2024-01-01"

pages_build_output_dir = "dist"

[env.production]
name = "brickhunt"

# Environment variables (set these in Cloudflare dashboard as encrypted secrets)
# REBRICKABLE_API_KEY - Your Rebrickable API key (server-side only, never exposed to client)
